// This is tests that tests everything that should be possible

Test.describe("strings + ?", func {
    assert "" + "a" == "a";
    assert "" + 1 == "1";
    assert "" + true == "true";
    assert "" + null == "null";
});