enum Result {
  Ok,
  Err,
}

func divide(a, b) {
  if b == 0 {
    return Result.Err("Cannot divide by 0");
  }

  return Result.Ok(a / b);
}

let value = divide(1, 0);

if value is Result.Err {
  print("Error!: " + value);
} else {
  print(value);
}